<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.wso2.employeeconcierge</groupId>
    <artifactId>payroll-agent</artifactId>
    <version>0.1.0</version>
    <packaging>jar</packaging>

    <name>Payroll Agent</name>
    <description>WSO2 Employee Concierge — Payroll Agent, a real A2A listener agent
        over gRPC, backed by a real Anthropic model via langchain4j.</description>

    <properties>
        <maven.compiler.release>17</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- 1.x line: needed for genuine supportedInterfaces (GRPC/JSONRPC/HTTP+JSON) —
             the 0.3.2.Final io.github.a2asdk line never emits that field. -->
        <a2a.sdk.v1.version>1.1.0.Final</a2a.sdk.v1.version>

        <quarkus.platform.version>3.26.1</quarkus.platform.version>
        <quarkus.langchain4j.version>1.3.1</quarkus.langchain4j.version>
        <!-- 4.34.2: the org.a2aproject.sdk 1.1.0.Final gRPC stubs are gencode'd
             against this newer protobuf runtime. Must be pinned alongside
             protobuf-java-util or JSON responses fail with
             NoSuchMethodError: JsonFormat$Printer.alwaysPrintFieldsWithNoPresence(). -->
        <protobuf.version>4.34.2</protobuf.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java</artifactId>
                <version>${protobuf.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java-util</artifactId>
                <version>${protobuf.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.a2aproject.sdk</groupId>
            <artifactId>a2a-java-sdk-client</artifactId>
            <version>${a2a.sdk.v1.version}</version>
        </dependency>
        <dependency>
            <groupId>org.a2aproject.sdk</groupId>
            <artifactId>a2a-java-sdk-reference-grpc</artifactId>
            <version>${a2a.sdk.v1.version}</version>
        </dependency>
        <!-- gRPC is the actual task-RPC binding (the only one listed in
             supportedInterfaces below). This is pulled in only because the
             A2A spec requires agent-card discovery over a plain HTTP
             well-known route regardless of RPC transport, and that route
             is registered by the REST reference module, not the gRPC one. -->
        <dependency>
            <groupId>org.a2aproject.sdk</groupId>
            <artifactId>a2a-java-sdk-reference-rest</artifactId>
            <version>${a2a.sdk.v1.version}</version>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.enterprise</groupId>
            <artifactId>jakarta.enterprise.cdi-api</artifactId>
            <version>4.1.0</version>
        </dependency>
        <dependency>
            <groupId>io.quarkiverse.langchain4j</groupId>
            <artifactId>quarkus-langchain4j-anthropic</artifactId>
            <version>${quarkus.langchain4j.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
