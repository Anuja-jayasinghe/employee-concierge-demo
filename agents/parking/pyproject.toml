[project]
name = "parking-agent"
version = "0.1.0"
description = "Parking Manager Agent — A2A listener agent, no LLM framework, REST binding"
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk==1.1.0",
    "uvicorn",
    "sse-starlette",
]

[tool.uv]
package = false
